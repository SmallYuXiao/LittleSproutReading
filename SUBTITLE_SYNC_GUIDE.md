# 字幕同步调整指南

## 🎯 功能说明

现在应用已经添加了**字幕时间轴调整**功能,可以手动微调字幕偏移量,让字幕与视频画面完全同步。

## 🎮 使用方法

### 1. 字幕偏移调节按钮

在视频播放器底部的控制条中,你会看到:

```
[-] 字幕: 0.0s [+]
```

- **[-] 按钮**: 字幕**提前** 0.5秒(字幕向前移动)
- **[+] 按钮**: 字幕**延后** 0.5秒(字幕向后移动)
- **中间显示**: 当前字幕偏移量

### 2. 调整步骤

1. **播放视频**,观察字幕与画面的同步情况

2. **判断偏移方向**:
   - 如果字幕**出现太早**(画面还没到): 点击 **[+]** 延后字幕
   - 如果字幕**出现太晚**(画面已经过了): 点击 **[-]** 提前字幕

3. **微调**: 每次点击调整 0.5秒,可以多次点击精确调整

4. **验证**: 找一句台词,确认字幕出现时间与演员说话时间一致

### 3. 常见情况

#### 情况A: 字幕整体偏移
- **现象**: 所有字幕都早/晚几秒
- **解决**: 使用 +/- 按钮调整到合适位置

#### 情况B: 字幕文件不匹配
- **现象**: 调整后仍然不同步,或者越往后越不准
- **解决**: 需要更换匹配的字幕文件

## 💡 技术细节

### 偏移量计算
```swift
adjustedTime = currentTime + subtitleOffset
```

- `subtitleOffset > 0`: 字幕延后(字幕出现更晚)
- `subtitleOffset < 0`: 字幕提前(字幕出现更早)

### 调整精度
- 每次调整: **±0.5秒**
- 可以修改为更精细的调整(如 ±0.1秒)

## 🔧 进阶功能(可选)

如果需要更精细的控制,可以添加:

1. **重置按钮**: 一键将偏移量重置为 0
2. **手动输入**: 直接输入偏移量数值
3. **保存设置**: 记住每个视频的偏移量设置

## 📝 注意事项

1. **偏移量会实时生效**,调整后立即看到效果
2. **偏移量不会保存**,重新打开应用需要重新调整
3. **最佳实践**: 找一句清晰的台词作为参考点进行调整
