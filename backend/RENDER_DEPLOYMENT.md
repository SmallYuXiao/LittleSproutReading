# 🚀 Render 部署指南

本指南将帮助你将 YouTube 字幕服务部署到 Render 云平台,完全免费!

## 📋 前置准备

1. **GitHub 账号** - 用于代码托管
2. **Render 账号** - 访问 [render.com](https://render.com) 注册(可用 GitHub 登录)
3. **代码推送到 GitHub** - 确保你的代码已上传到 GitHub 仓库

---

## 🎯 部署步骤

### 步骤 1: 推送代码到 GitHub

如果还没有推送代码,执行以下命令:

```bash
# 在项目根目录
git add .
git commit -m "准备 Render 部署"
git push origin main
```

### 步骤 2: 在 Render 创建 Web Service

1. 访问 [Render Dashboard](https://dashboard.render.com/)
2. 点击 **"New +"** → 选择 **"Web Service"**
3. 连接你的 GitHub 仓库:
   - 点击 **"Connect GitHub"** 授权 Render 访问
   - 选择 `LittleSproutReading` 仓库
4. 配置服务:

   | 配置项 | 值 |
   |--------|-----|
   | **Name** | `littlesprout-backend` (或你喜欢的名字) |
   | **Region** | `Singapore` (离中国最近) |
   | **Branch** | `main` |
   | **Root Directory** | `backend` ⚠️ **重要!** |
   | **Runtime** | `Python 3` |
   | **Build Command** | `pip install -r requirements.txt` |
   | **Start Command** | `gunicorn --bind 0.0.0.0:$PORT --workers 1 --threads 2 --timeout 120 app:app` |

5. 选择 **Free** 套餐
6. 点击 **"Create Web Service"**

### 步骤 3: 等待部署完成

- Render 会自动构建和部署你的应用
- 首次部署大约需要 **3-5 分钟**
- 你可以在 **Logs** 标签页查看部署进度

### 步骤 4: 测试你的 API

部署成功后,Render 会提供一个 URL,格式类似:
```
https://littlesprout-backend.onrender.com
```

测试健康检查接口:
```bash
curl https://你的应用名.onrender.com/health
```

测试字幕 API:
```bash
curl https://你的应用名.onrender.com/api/subtitles/dQw4w9WgXcQ
```

---

## 🔧 配置说明

### 自动部署配置 (render.yaml)

项目中的 `render.yaml` 文件包含了所有部署配置,Render 会自动识别并使用。

### 环境变量

如果需要添加环境变量:
1. 在 Render Dashboard 中打开你的服务
2. 进入 **"Environment"** 标签页
3. 添加环境变量,例如:
   - `PYTHON_VERSION`: `3.11.0`
   - 其他 API 密钥等

---

## ⚠️ 免费套餐限制

### 休眠机制
- **15 分钟无活动**后服务会自动休眠
- 下次访问时需要 **30 秒左右**唤醒
- 唤醒后性能正常

### 解决方案
如果不想休眠,可以:
1. 使用 **UptimeRobot** 等服务每 10 分钟 ping 一次你的 `/health` 接口
2. 升级到付费套餐($7/月)

### 其他限制
- **750 小时/月**免费运行时间(足够个人使用)
- **100 GB 带宽/月**
- 单个请求最大 **100 秒**超时

---

## 📱 更新 iOS 应用配置

部署成功后,需要更新 iOS 应用中的 API 地址:

### 方法 1: 修改 Config.plist

打开 `LittleSproutReading/Config.plist`,更新 `BackendURL`:

```xml
<key>BackendURL</key>
<string>https://你的应用名.onrender.com</string>
```

### 方法 2: 使用环境变量

如果使用 `.env` 配置,更新:
```
BACKEND_URL=https://你的应用名.onrender.com
```

---

## 🐛 常见问题

### 1. 部署失败 - "Build failed"
**原因**: 依赖安装失败  
**解决**: 检查 `requirements.txt` 是否正确,确保所有包都能安装

### 2. 服务启动失败 - "Application failed to respond"
**原因**: 端口配置错误  
**解决**: 确保 `app.py` 使用 `os.getenv('PORT')` 读取端口

### 3. API 返回 404
**原因**: Root Directory 配置错误  
**解决**: 在 Render 设置中确认 **Root Directory** 设置为 `backend`

### 4. 首次访问很慢
**原因**: 服务休眠,正在唤醒  
**解决**: 这是正常现象,等待 30 秒即可

### 5. YouTube API 调用失败
**原因**: 某些 YouTube 功能可能被限制  
**解决**: 
- 检查 Render 日志查看具体错误
- 考虑使用代理或其他解决方案

---

## 🔄 自动部署

配置完成后,每次你推送代码到 GitHub:
```bash
git push origin main
```

Render 会**自动检测并重新部署**你的应用! 🎉

---

## 📊 监控和日志

### 查看日志
1. 进入 Render Dashboard
2. 选择你的服务
3. 点击 **"Logs"** 标签页
4. 实时查看应用日志

### 查看指标
- **"Metrics"** 标签页可以看到:
  - CPU 使用率
  - 内存使用
  - 请求响应时间

---

## 🎉 完成!

现在你的 YouTube 字幕服务已经部署到云端,可以:
- ✅ 24/7 在线访问
- ✅ 自动 HTTPS 加密
- ✅ 全球 CDN 加速
- ✅ 自动部署更新

享受你的云端服务吧! 🚀

---

## 📞 需要帮助?

如果遇到问题:
1. 查看 Render 的 [官方文档](https://render.com/docs)
2. 检查项目的 GitHub Issues
3. 查看 Render 服务的日志输出
